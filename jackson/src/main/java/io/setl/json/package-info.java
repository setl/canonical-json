/**
 * Jackson integration to generate canonical form JSON.
 *
 * <p>To use this, simply create your Jackson ObjectMapper as follows:</p>
 *
 * <p><code>ObjectMapper mapper = new ObjectMapper(new CanonicalFactory());</code></p>
 */
package io.setl.json;